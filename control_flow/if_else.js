// if

const isUserloggedIn = true;
if(isUserloggedIn){

}
else{
  
}

// <, >, <=,==,===,!=(2!=3) => correct!
/*
we use the if else for teh control flow which code run at the spcific match or not so can contaol the flow of the code
*/

// for both the conditions are true 
const userLoggedIn = true;
const debitCard = true;
const loggggoogle = false;
if(userLoggedIn && debitCard || loggggoogle){
console.log("Allow to buy course ");
}