// arrays
const myArr = [1,2,3,4,4,5,8];
// accseeing arrau by the index ad index start of 0 
console.log([0])
// ## array methods ##
// length of the array 
console.log(Array.myArr)
myArr.push(6) // add valuse at the alst 
myArr.pop() // remove last 

myArr.unshift(90) // adding at the starting 
myArr.shift() // right shif teh array and remove firtst elm
console,log(myArr.includes(4));
console.log(indexeof(4));

let newArr = myArr.join() // conver teh array in to teh single str 

// slice and splice

const myn1 = myArr.slice(1,3);// it gives the sub arra and from starting index to teh last -1 index

console.log(myArr.splice(1,4)); // manuplate the original array and return teh last elemnt too unlike in slice last aaray not includde

